# Apple II Floppy Disk Emulator for Raspberry Pi Pico 2

Високопроизводителен емулатор на Apple II Disk II флопи дискове, базиран на Raspberry Pi Pico 2 (RP2350). Този проект позволява да използвате SD карта като замяна на оригиналните 5.25" флопи дискети за Apple II компютри.

## 🎯 Основни функции

- **Пълна съвместимост** с Apple II Disk II контролери
- **Поддръжка на два формата**:
  - `.dsk` файлове (143KB) - стандартен формат
  - `.nic` файлове (280KB) - GCR-кодиран формат
- **Реално време** - използва PIO и DMA за прецизно генериране на сигнали
- **Четене и запис** - поддръжка на пълни read/write операции
- **SD карта поддръжка** - FAT32 файлова система, поддръжка на големи карти (до 64GB+)
- **Графичен интерфейс** - OLED/LCD дисплей с навигация чрез ротационен енкодер
- **CLI интерфейс** - сериен терминал за управление и диагностика

## 🔧 Хардуерни изисквания

### Микроконтролер
- **Raspberry Pi Pico 2** (RP2350) с ARM Cortex-M0+ ядра
- Работи на 200MHz (overclock от 150MHz)

### Дисплей (изберете един)
- **SSD1306** (128x64, I2C) - по подразбиране
- **SSD1309** (128x64, I2C) - с цветен status bar
- **SH1107** (128x128, I2C) - по-голям дисплей
- **MSP1601/SSD1283A** (128x128, SPI) - LCD дисплей

### Други компоненти
- **SD карта модул** (SPI интерфейс)
- **Ротационен енкодер** (за навигация в UI)
- **Съответствие с Apple II Disk II интерфейс** (PH0-PH3, READ, WRITE, WRITE_ENABLE, DRIVE_SEL)

## 📋 GPIO Pinout

### Apple II Floppy Interface
```
PH0 (Phase 0)        → GPIO 6
PH1 (Phase 1)        → GPIO 7
PH2 (Phase 2)        → GPIO 8
PH3 (Phase 3)        → GPIO 9
READ (Data Output)   → GPIO 10
WRITE (Data Input)   → GPIO 11
WRITE_ENABLE         → GPIO 12
DRIVE_SEL            → GPIO 13
```

### SD Card (SPI)
```
MISO                 → GPIO 16
CS                   → GPIO 17
SCK                  → GPIO 18
MOSI                 → GPIO 19
Card Detect          → GPIO 15 (active LOW)
```

### Display (I2C за SSD1306/SH1107)
```
SDA                  → GPIO 20
SCL                  → GPIO 21
RESET (optional)     → GPIO 22
```

### Display (SPI за MSP1601)
```
MOSI                 → GPIO 14
SCK                  → GPIO 15
CS                   → GPIO 22
DC (Data/Command)    → GPIO 23
RST                  → GPIO 24
LED (Backlight)      → GPIO 25
```

### Rotary Encoder
```
CLK                  → GPIO 26
DT                   → GPIO 27
SW (Button)          → GPIO 28
```

### CLI UART
```
TX                   → GPIO 4
RX                   → GPIO 5
Baudrate             → 115200
```

## 🚀 Компилация и инсталация

### Предварителни изисквания

1. **Raspberry Pi Pico SDK** (версия 2.2.0 или по-нова)
2. **CMake** (версия 3.13 или по-нова)
3. **ARM GCC toolchain** (версия 14.2 или по-нова)
4. **Python 3** (за pico-sdk инструменти)

### Стъпки за компилация

```bash
# Клонирайте проекта
git clone <repository-url>
cd FLOPPY_APPLE_II_PICO

# Създайте build директория
mkdir build
cd build

# Конфигурирайте с CMake
cmake ..

# Компилирайте
make -j4

# Резултатът е FLOPPY_APPLE_II_PICO.uf2 файл в build/ директорията
```

### Инсталация на Pico

1. Задръжте **BOOTSEL** бутона на Pico 2
2. Свържете USB кабела към компютъра
3. Копирайте `FLOPPY_APPLE_II_PICO.uf2` файла в появилия се USB drive
4. Pico 2 ще се рестартира автоматично

## 💾 Подготовка на SD карта

1. **Форматирайте SD картата** като FAT32
2. **Създайте директория** за диск образи (например `/DISKS/`)
3. **Копирайте `.dsk` или `.nic` файлове** в директорията
4. **Поставете картата** в SD карт модула

### Поддържани формати

- **DSK файлове**: 143,360 байта (35 tracks × 16 sectors × 256 bytes)
- **NIC файлове**: 286,720 байта (35 tracks × 16 sectors × 512 bytes, GCR-кодирани)

## 🎮 Използване

### Графичен интерфейс (UI)

Емулаторът автоматично стартира с графичен интерфейс:

- **Ротационен енкодер**: Навигация нагоре/надолу
- **Бутон на енкодера**: Избор/потвърждение
- **Екрани**:
  - **File List**: Списък с файлове на SD картата
  - **Info**: Информация за заредения файл (тип, размер, track, cache status)
  - **Status**: Статус на SD картата (тип, скорост, размер, partition info)

### CLI интерфейс

Свържете се чрез сериен терминал (115200 baud):

```
Available commands:
  help              - Show help
  load <file>       - Load disk image from SD card
  list              - List files in current directory
  cd <dir>          - Change directory
  pwd               - Print current directory
  info              - Show disk image info
  status            - Show emulator status
  seek <track>      - Seek to track (0-34)
  read <t> <s>      - Read track and sector
  gpio/pins         - Show GPIO pin states
  test              - Test emulator
```

## 🔬 Технически детайли

### Архитектура

- **Core 0**: Floppy емулация (real-time сигнали, PIO/DMA)
- **Core 1**: UI управление, SD карта операции, CLI

### GCR Encoding/Decoding

- **DSK файлове**: Автоматично кодиране/декодиране на GCR формат
- **NIC файлове**: Директно използване на GCR-кодирани данни

### Stepper Motor Tracking

- **GPIO IRQ**: Реално време проследяване на фазите на стъпковия мотор
- **Висок приоритет**: Гарантира, че не се изпускат фази
- **Автоматично проследяване**: Позицията се обновява автоматично

### Оптимизации

- **PIO + DMA**: Хардуерно генериране на битови сигнали
- **Track Cache**: Кеширане на текущата пътечка в паметта
- **Lazy Loading**: Пътечките се зареждат само когато е необходимо
- **Direct Block Access**: За NIC файлове - директно четене на сектори

## 📊 Спецификации

### Apple II Disk II Формат
- **Tracks**: 35 (0-34)
- **Sectors per track**: 16
- **Bytes per sector**: 256 (raw data)
- **Bit period**: 4μs (125 kbps)
- **Rotation speed**: 300 RPM (200ms per rotation)
- **GCR bytes per track**: 6,656 (416 bytes per sector)

### Производителност
- **Read latency**: < 10ms за NIC файлове
- **Write support**: Пълна поддръжка
- **Track change**: Автоматично проследяване

## 🐛 Известни проблеми и ограничения

- SD картата трябва да е форматирана като FAT32
- Поддръжката за MBR партиции е ограничена (търси първата партиция)
- exFAT/NTFS файлови системи не се поддържат (показва се съобщение за форматиране)

## 🔧 Конфигурация

### Промяна на дисплей

Редактирайте `PinConfig.h`:

```c
// За SH1107 (128x128)
#define USE_SH1107

// За SSD1306 (128x64)
// #define USE_SH1107  // Коментирайте това

// За MSP1601 (128x128, SPI)
// #define USE_MSP1601
```

### Промяна на GPIO пинове

Всички GPIO пинове са дефинирани в `PinConfig.h`. Променете ги според вашата схема.

## 📝 Лиценз

[Добавете лиценз информация тук]

## 🙏 Благодарности

- Raspberry Pi Foundation за Pico SDK
- Общността на Apple II за поддръжка и тестване

## 📧 Контакт

[Добавете контактна информация тук]

---

**Версия**: 0.1  
**Платформа**: Raspberry Pi Pico 2 (RP2350)  
**SDK версия**: 2.2.0

